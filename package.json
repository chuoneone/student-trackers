{
  "name": "student-grade-tracker",
  "version": "1.0.0",
  "description": "Firebase Student Grade Tracker frontend",
  "scripts": {
    "push-frontend": "git add . && git commit -m \"update: automated sync\" && git push origin main",
    "deploy": "npm run push-frontend"
  }
}
